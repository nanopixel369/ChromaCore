# Decisions (Append-only ADR Log)

Use ADRs for irreversible/high-cost choices (interfaces, persistence formats, coordinate math, plugin API constraints).

- ADR-{{YYYYMMDD}}-<slug>
  Context: <why this decision exists>
  Decision: <what was chosen>
  Consequences: <tradeoffs and follow-ons>
  Verification: <how we know it works; commands/checks>
